# Roadmap

## v0.1 – Foundation (Done)
- Central umbrella repository created
- Unified README + architecture diagram
- Clear mapping of all existing layers

## v0.2 – Integration (Next)
- `docker-compose.fullstack.yml` for one-command demo
- `starter-kit/` template that wires every layer together
- Shared npm packages (`@observatory/logger`, `@observatory/healthcheck`, etc.)

## v0.3 – Polish & Scale
- Full TypeScript migration across the suite
- Kubernetes manifests (EKS-ready)
- OpenTelemetry traces + Jaeger
- Docusaurus documentation site (auto-pulling from repos)

## Future
- Layer 1 frontend repo
- Layer 10 orchestration (ArgoCD / Helm)
- Official Helm chart + Terraform module registry
