global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: 'console'

receivers:
  - name: 'console'
    # For demo: prints alerts to console
    # You can later add email, Slack, Teams, etc.